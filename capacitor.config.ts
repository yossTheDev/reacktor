import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yossthedev.reacktor',
  appName: 'reacktor',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
